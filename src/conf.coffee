'use strict'

angular.module 'app'

# Default size of Mockups (needed for translating)
.constant("DEFAULT_MOCKUP_SIZE", {
  width:  1000
  height: 1000
})

# First state to navigate to
.constant("DEFAULT_STATE", '1')